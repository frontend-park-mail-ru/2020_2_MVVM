<fest:template xmlns:fest="http://fest.mail.ru" context_name="items">
    <fest:if test="!items">
        <span id="add_to_prefer"><i class="fas fa-save"></i>Добавить в избранное</span>
    </fest:if>
    <fest:if test="items">
    <span id="delete_from_prefer"><i class="fas fa-save"></i>Убрать из избранного</span>
    </fest:if>
</fest:template>